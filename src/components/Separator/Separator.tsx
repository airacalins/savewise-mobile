import React from "react";
import { View } from "react-native";

import { defaultStyles } from "../../layouts/DefaultStyles";

export const Separator = () => {
  return <View style={defaultStyles.dottedSeparator} />;
};
